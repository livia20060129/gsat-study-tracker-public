# 最新更新

版本：公開版 v171.6.0-public.27

## 移除每日統計的數學頁數

- 每日統計移除「今日數學頁數」與「本週數學頁數」，不再顯示三段切換滑塊。
- 左側統計卡只保留「今日完成時間」科目圓環，右側完成率區塊維持原有內容與排列。
- 同步移除這兩個畫面的切換事件、專屬跨日頁數索引與背景摘要計算，避免保留不可見的工作。
- 教材卡片中的實際頁碼、Calendar 教材資訊與教材進度圖仍完整保留，不受本次調整影響。

## 驗證

- 394 項單元／回歸測試通過，包含每日統計不再產生兩個數學頁數項目的檢查。
- 12 項 Chromium 瀏覽器流程皆通過，其中新增驗收確認首頁只顯示今日完成時間圓環。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`refactor(public): remove daily math page statistics`

---

版本：公開版 v171.6.0-public.26

## 起床／就寢時間與個人狀態

- 參考個人版 v171.6.21，將今日資訊的起床時間改為「起床／就寢」共用切換欄位，切換時保留兩邊尚未儲存的草稿並維持固定高度。
- 切換模式本身不寫入儲存空間；時間欄位仍沿用公開版「離開欄位才儲存」的規則。
- 就寢時間保存完整日期時間；凌晨 00:00～05:59 歸為隔日凌晨，06:00 起歸為當日，避免只保存時鐘數字後無法判斷跨日。
- 資料格式升級至 schema 2；舊版 schema 0／1 會自動升級，但不會替舊資料猜測或補造就寢時間。
- 週／月總結新增獨立「個人狀態」，顯示平均睡眠、平均就寢、平均起床、有效夜晚數、每日睡眠趨勢與上期比較。
- 睡眠時間使用前一日就寢和當日起床計算；資料缺漏、不合法、非正值或超過 18 小時的組合不列入平均，且作息欄位不影響學習時間與完成率。
- JSON 匯入會驗證就寢時間、跨日旗標與日期是否一致；週摘要匯出也會標示就寢時間及「隔日凌晨」。

## 驗證

- 394 項單元／回歸測試通過，包含跨日、睡眠平均、資料匯入、schema 升級及學習統計隔離。
- 12 項 Chromium 瀏覽器流程測試通過；新增作息切換、離開欄位儲存、手機固定高度與個人狀態驗收。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`feat(public): add bedtime tracking and sleep summaries`

## 結算完成率出現時的數學面板置中

- 修正右側顯示結算完成率、把整列高度撐高後，左側數學頁數動畫容器仍停留在固定最小高度而顯得偏上的問題。
- 動畫容器現在會填滿統計卡的剩餘高度，今日／本週數學頁數都以完整可用高度垂直置中。
- 數字、進度條與百分比仍共用左側基準線；原本的高度變化、滑入與淡入動畫均保留。
- 瀏覽器驗收固定切換至會顯示結算完成率的星期日，直接量測容器底部及內容中心位置。

## 驗證

- 383 項單元／回歸測試通過。
- 11 項 Chromium 瀏覽器流程測試通過，包含「結算完成率顯示中」的數學面板幾何驗收。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`fix(public): center math metrics when settlement is visible`

## Azar 教材進度改為大章分段

- 教材進度圖的 Azar 英文文法由 149 個小節段落改成 14 個大章段落。
- 每段顯示章名及完整頁碼範圍，例如「第一章：現在式（p.1–29）」。
- Calendar 與 Tracker 仍保留原本的小節完成資料；完成一個小節時，只依該小節實際涵蓋頁數增加所屬大章進度，不會把整章直接標成完成。
- 不修改已儲存資料格式，也不影響 Tracker 中原本的小節卡片。

## 驗證

- 383 項單元／回歸測試通過。
- 11 項 Chromium 瀏覽器流程測試通過。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`feat(public): group Azar material progress by chapter`

## 數學頁數面板實際套用修正

- 修正上一版差異更新資料夾未包含 `index.html`，導致額外內層容器沒有一起覆蓋的問題。
- 「今日數學頁數」與「本週數學頁數」現在直接由原有面板垂直置中，不再依賴新增的 HTML 容器。
- 數字、進度條及百分比直接對齊面板左側；面板高度變化與原本的滑入淡入動畫維持不變。
- 瀏覽器驗收改為量測面板的直接子元素，避免只驗證不存在於舊首頁的輔助容器。

## 驗證

- 383 項單元／回歸測試通過。
- 11 項 Chromium 瀏覽器流程測試通過，包含兩個數學頁數面板的實際幾何量測。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`fix(public): apply math metric alignment without extra markup`

## 手機連線設定穩定化

- 連線設定在手機版展開時固定於原本的畫面位置，保留等高佔位，不再推動後方內容造成跳動。
- 展開、內容高度更新與收合共用同一套高度過渡；收合完成前不會提前隱藏內容。
- 背景頁面保留原捲動位置，連線面板內仍可獨立捲動，並保留原本的展開／收合動畫。

## Calendar 返回與啟動保護

- 返回 Tracker 或登入後，先顯示今天實際儲存的完整紀錄，不先用尚未完成載入的排程重建卡片。
- Cloud 紀錄載入後仍暫停排程比對；只有 Calendar 狀態及任務快取成功完成後才重新比對並刷新。
- Cloud 紀錄讀取設為 20 秒上限、Calendar 狀態讀取設為 15 秒上限；逾時或失敗均保留目前紀錄。
- 正在輸入而延後刷新時，會連同「暫不比對排程」選項一起保留，離開欄位或按立即套用後才安全更新。

## 自然科「新關鍵」頁碼地圖

- 新增生物《新關鍵》p.4–236 與化學《新關鍵》p.2–281 的連續頁碼對照。
- 手動紀錄生物或化學「新關鍵」時，頁碼下方會顯示對應單元與主題。
- 教材進度圖的「我的教材」新增生物、化學「新關鍵」，以大主題彙整進度，但完成比例仍依實際頁碼計算。

## 保留既有數學面板

- 「今日數學頁數／本週數學頁數」仍維持面板內垂直置中。
- 數字、進度條與百分比仍靠左對齊，切換動畫不變。

## 驗證

- 383 項單元／回歸測試通過。
- 11 項 Chromium 瀏覽器流程測試通過，包含手機捲動位置與展開面板幾何驗收。
- TypeScript 型別檢查與 Vite 正式建置通過。

## Commit 建議

`feat(public): stabilize mobile connections and add New Key maps`

## 數學頁數面板對齊

- 「今日數學頁數」與「本週數學頁數」的內容改為在面板中垂直置中。
- 兩個面板使用相同的上下內距，移除原本因不對稱內距造成的 4px 垂直偏移。
- 數字、進度條與百分比仍維持靠左對齊，並共用相同的左側基準線。
- 新增 Chromium 幾何驗收，實際量測兩個面板的垂直中心與各內容左邊界。
- 驗證結果：正式建置、372 項單元測試與 11 項 Chromium 瀏覽器測試全部通過。

## 全專案程式碼檢查與可讀性整理

- 檢查 `src`、Supabase Edge Functions、單元測試與瀏覽器測試，共 120 個 TypeScript 檔案、20,310 行。
- 將 typed code 的相對 ES module 匯入統一補上 `.ts` 副檔名，避免解析規則因執行環境而異。
- 移除巢狀三元運算與無效重複條件，改用具名輔助函式、`if/else` 或 `switch`，維持原本輸出與判斷順序。
- 補齊所有頂層函式的明確回傳型別；typed code 沒有 `any`、`@ts-ignore` 或 `@ts-expect-error`。
- 整理教材頁碼標籤、實際頁碼邊界、英文訂正列、週期比較與 Google Calendar 同步結果等重複邏輯。
- `legacy-app.ts` 仍保留既有 `@ts-nocheck` 相容層，這次只統一匯入規格，不進行可能改變執行順序的大規模拆寫。
- 本版不修改資料格式、Supabase schema、Calendar 判斷、畫面功能或使用者紀錄。
- 驗證結果：TypeScript typecheck、正式建置、372 項單元測試與 10 項 Chromium 瀏覽器測試全部通過。

## Calendar Prompt 的物理「逆轉勝」頁碼

- 移除「只有 p.1–255 全書範圍、不可建立單元頁界」的過期說明。
- 改列已核對的 p.3–255 完整頁碼地圖，共 37 個單元主題／題型區段。
- 自然科排程規則改為直接依「逆轉勝」實際主題頁碼填寫，不再沿用《123日的淬鍊》頁碼。
- 教材識別碼維持 `GSAT-PHYS-NIZHUANSHENG`，並加入 Prompt 與程式頁碼地圖一致性測試。

## 英文寫作測驗分類

- 「英文寫作測驗」改列入英文教材的「學測」分類。
- 手動新增英文項目與英文複習項目的教材選單同步更新。
- 教材進度圖「我的教材」的英文清單同步將此教材移至「學測」，不再列於「其他」。
- 教材 ID、既有勾選紀錄與進度資料皆保持不變。

## 英文文法總複習分類

- 「英文文法總複習講義」改列入英文教材的「學測」分類。
- 手動新增英文項目與英文複習項目的教材選單同步更新。
- 教材進度圖「我的教材」的英文清單同步將此教材移至「學測」，不再列於「其他」。
- 教材 ID、既有勾選紀錄與頁碼進度資料皆保持不變。

## 科目分配的自然科合併與下鑽

- 科目分配總覽將物理、化學、生物、地科的完成時間合併為單一「自然」圓環區段。
- 點入自然後，明細圓環重新以不同顏色顯示物理、化學、生物、地科，並在環上標註「物／化／生／地」。
- 右側文字清單仍保留細科名稱、每個實際學習項目、占比與時數，不會因總覽合併而遺失分項。
- 底層紀錄仍保存原本四科，合併只套用於科目分配總覽。

## 週曆／月曆圖例

- 100% 完成圖例改為「亮綠完整圓環＝100% 完成」，與實際 `#70DE43` 外圈一致。
- 新增紅色圓心＝身體不適、橘色圓心＝疲倦（較疲累）、黃色圓心＝外出的狀態說明。

## 外出／身體不適日的總結統計

- 外出或身體不適當天仍照實顯示已完成進度、學習時間與當日完成率，不會把當日紀錄隱藏。
- 這兩種狀態的當日完成率不列入週／月彙總、與上期比較及固定小結評語。
- 學習時間仍照常列入週／月科目分配與學習趨勢，只有完成率彙總會排除。
- 日期提示會明確標示「本日完成率不列入週／月統計」。

## 對話式數學講義與頁碼地圖

- 分冊講義新增「對話式」1、2、3A、4A，共用 PDF 目錄核對完成的細分單元／章節頁碼。
- 複習講義新增「對話式複習講義」1～2、3A～4A；1～2 冊正文對照 p.1–164，3A～4A 冊正文對照 p.1–188，不把後方課後練習本算入教材進度。
- 手動頁碼提示、教材進度圖與 Google Calendar 標準備註都能辨識兩種對話式教材名稱及冊別。
- 既有「教學講義」資料保留，舊紀錄不會被重新命名或遺失。

## 英文教材用途分組

- 英文閱讀與英文複習選單統一分成「學測」與「課外補充」。
- 「學測」固定收錄：學測週計畫、混合題30篇實戰演練、ACE Reading、大考英聽A攻略、英文寫作測驗、兩本主題百匯、英文文法總複習講義，以及兩冊英文字彙王。
- 其餘英文教材全部列入「課外補充」；不恢復「新增講義／其他講義」這類依新增時間分類。
- 分組只調整顯示，教材實際儲存名稱保持不變。

## 教材進度圖的「我的教材」分組

- 數學勾選清單依教材範圍分為「分冊講義／複習講義」。
- 英文勾選清單依用途分為「學測／其他」。
- 分組與新增項目選單使用相同分類規則，原有教材 ID 與勾選紀錄不變。

## 所有教材移除新增／其他分類

- 英文閱讀與英文複習教材改為單一選單，不再顯示「新增講義／其他英文項目」。
- 自然科維持依科目直接列出所有可用教材。
- 數學不再依新增／其他分類，只保留「分冊講義／複習講義」這項教材範圍分類。
- 教材名稱與儲存值皆未更動，既有紀錄仍可直接使用。

## 手動新增教材選單重新分類

- 自然科講義移除「新增講義／其他講義」分組，選定科目後直接顯示該科所有可用教材。
- 尚未選擇自然科目時，只顯示「請先選擇科目」，避免先顯示不相符教材。
- 數學講義改分為「分冊講義」與「複習講義」。
- 「教學講義」列入分冊講義；「智慧型、新關鍵、新大滿貫、複習週記」列入複習講義。
- 教材的實際儲存值沒有更動，既有紀錄與 Calendar 內容維持相容。

## 物理「逆轉勝」頁碼地圖

- 依紙本目錄建立 p.3–255 的完整頁碼對照，共 37 個主題／題型區段。
- 單元1～9皆細分至各主題，素養導向進階試題細分為「探究實作」與「跨科整合」。
- 紀錄卡輸入頁碼後會顯示實際對應單元與主題，不再顯示「來源未提供各單元頁界」。
- 教材進度圖改以各主題實際頁數計算填色完成比例。
- 加入頁碼連續性、第一段、波動與光學及末兩段題型的自動驗收。

## 主頁分頁名稱修正

- 主頁瀏覽器分頁名稱移除「v171」，統一顯示「每日讀書完成度紀錄卡」。
- 教材進度頁維持相同分頁名稱。
- 新增 Playwright 驗收，防止主頁分頁名稱再次混入版本代號。

## 我的教材收合與自然分組

- 「我的教材」面板預設收合；點擊標題可展開或再次收合，並加入與連線設定一致的高度、透明度與位移過渡。
- 自然教材依「物理、化學、地科、生物」顯示小標與各自教材。
- 自然教材選項只顯示書名，不再重複「自然｜科目」。
- 既有教材 ID 與儲存鍵未變更，原本勾選的教材會繼續保留。
- 教材進度頁的瀏覽器分頁名稱改為「每日讀書完成度紀錄卡」。
- 新增 Playwright 驗收，檢查收合完成時機、四科順序及物理教材內容。

## 公開版 Google OAuth 設定對齊

- Calendar OAuth 改用權限較小的 `calendar.events.readonly`，與 Google Cloud 公開版專案設定一致。
- 隱私權政策同步說明實際申請的權限與讀取內容。
- Supabase 新連線的 scope 預設值改為 `calendar.events.readonly`。
- README 全面改用公開網域 `gsat-study-tracker.liviayeh.dev`，並補上 Cloudflare 建置變數設定。
- 新增公開版 OAuth scope 與正式網域防回歸測試。

## 線上 OAuth 設定套用

- Google Cloud 公開版專案已建立獨立 Web OAuth Client。
- Google OAuth 應用程式名稱已統一為「每日讀書完成度紀錄卡」，服務條款已改用公開版 `/terms`。
- Supabase Edge Function 已更新 `GOOGLE_CLIENT_ID` 與 `GOOGLE_CLIENT_SECRET`。
- GitHub Actions 與 Cloudflare Builds 已改用同一組公開版 `VITE_GOOGLE_CLIENT_ID`。
- Cloudflare 已重新建置並成功部署；公開網址已可正常顯示「連接 Google Calendar」。

## GitHub Pages 重新部署修復

- Pages artifact 改用 GitHub 官方固定名稱 `github-pages`，不再把名稱綁定 `github.run_attempt`。
- 修正按「Re-run failed jobs」後，部署步驟找不到先前成功 build 所上傳 artifact 的問題。
- Node 20 與 `punycode` 訊息皆為 Action 相依套件警告，不是本次部署失敗原因。

## Supabase Project Ref 單一來源

- GitHub Environment／Repository 兩處 `SUPABASE_PROJECT_ID` 已統一為公開版 Project Ref，避免舊的自用版設定被誤用。
- 發布流程只從通過一致性測試的 `supabase/config.toml` 取得 Public Project Ref。

## Public Supabase 指向修復

- 修復更新資料夾漏帶 `supabase/config.toml`，導致 GitHub `SUPABASE_PROJECT_ID` 與 repository 設定不一致。
- 新增 Public 專案綁定測試，統一核對 Supabase CLI、前端、每小時 Calendar workflow 與 OAuth callback 文件。

## CI 混合版本修復

- 修復 GitHub 上 `package.json` 仍為 `public.2`、但 runtime 誤混入個人版固定排程邏輯的版本不一致問題。
- 修復包同時收錄正確的 Public runtime、兩項最新功能及 Public 專屬防回歸測試，避免只覆蓋單一檔案後再次帶回內建排程。

## 同步個人版最新功能

- 學習總結的「身體不適／較疲累／外出」狀態色，會依當日學習時間調整透明度，並與完成率外圈分離。
- Cloud／Calendar 設定面板新增平順的展開高度過渡；收合、手機底部面板與減少動態效果設定維持原有行為。

## 公開版 Supabase 分離

- 公開版已改用獨立 Supabase 專案 `xcpnxkkixsxzgbqsiuud`。
- 前端 API、Supabase CLI、每小時 Calendar 同步與 OAuth callback 文件已統一指向公開版專案。
- 部署 workflow 會比對 GitHub `SUPABASE_PROJECT_ID` 與 `supabase/config.toml`，避免誤部署到自用版後端。

## 公開版分流

- 建立獨立的 `gsat-study-tracker-public` 專案，自用版未修改。
- Cloudflare Worker 名稱改為 `gsat-study-tracker-public`，避免部署時覆蓋自用版。
- 清除星期別、固定日期及硬編碼 Calendar fallback；只有已連線並完成同步的 Google Calendar 可辨識項目會自動加入。
- 手動新增項目仍然保留。

## 我的教材

- 教材進度頁新增「我的教材」。
- 使用國文／英文／數學／自然滑塊查看既有教材，並以勾選決定要追蹤的教材。
- 教材選擇依訪客或登入帳號分開保存。
- 教材進度圖只顯示已勾選的教材；未選擇時顯示空白提示。

## 驗證

- TypeScript 型別檢查通過。
- 369 項單元／回歸測試通過。
- 10 項 Playwright 真實瀏覽器測試通過，包含公開版空白起始狀態與教材勾選保存。

## Commit 建議

`feat(materials): add Dialogue math maps and group English books`
