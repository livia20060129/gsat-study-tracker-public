# GSAT Study Tracker

目前版本：**公開版 v171.6.0-public.9**

公開版學測讀書追蹤器，整合 Google Calendar 唯讀排程、手動新增項目、Supabase 跨裝置同步、計時、完成率、教材進度與學習總結。

> 此資料夾是與自用版完全分開的公開版本。公開版不建立星期別、固定日期或內建教材行程；Tracker 只加入實際從 Google Calendar 讀到且能辨識的排程，使用者仍可手動新增項目。

## I. 使用方式

1. 開啟 Tracker，選擇日期後填寫今日狀態與各項進度。
2. 每張可計時卡片可點一下「手動／計時」切換；計時顯示分：秒，完成後換算為分鐘並四捨五入到小數點後一位。
3. 勾選項目、修改欄位或輸入文字時，內容會先保存到目前瀏覽器；重要的勾選、延期與刪除會立即保存，連續打字會短暫合併後再保存，減少手機卡頓。
4. 按「儲存紀錄」後，按鈕會分別顯示：
   - `已存本機 ✓`：本機已安全保存，但 Cloud 尚未完成。
   - `Cloud 已同步 ✓`：Supabase 已確認寫入完成。
   - `同步失敗`：本機仍保留，恢復網路後會立即重試。
5. 登入 Cloud 後，可在不同裝置讀取同一帳號的紀錄；同時開啟多個分頁時，系統會先比較再上傳。
6. 整張自行新增卡片刪除前會再次確認；單字列、雜誌列與其他小項目刪除後可在 8 秒內按「復原」。
7. 「匯出與匯入」可先預覽 JSON，確認後才寫入，並可復原上次匯入。

## II. 目前版本的重要功能及解說

### 1. 今日項目與本週項目

- 今日／本週使用滑塊切換。
- 公開版不含任何內建固定排程；今日／本週項目只來自 Google Calendar 與使用者手動新增。
- 同科目相鄰排列，科目使用低飽和淡色區分。
- Calendar 頁碼範圍優先於單元推算；沒有頁碼範圍時才使用單元進度。
- 相同連續範圍可合併；中斷範圍或不同回次保留為同一大卡中的獨立子項目。

### 2. 延期與完成率

- 延期必須選擇目標日並按「確認延期」後才生效。
- 每個目標日預設最多 3 項，超過時會再次詢問。
- 確認延期後，原訂今日項目與今日總項目的分母都會減少一項，不會把延期誤算成完成。
- 子卡片可分別延期、完成、手動填入時間或計時。
- 未延期項目若在原日期之後才手動勾選，會保存實際勾選日；同日或提前勾選不另外記錄日期，取消勾選會清除紀錄。
- 延期項目在延期日完成時，延期卡會保存實際完成日，並同步勾選原日期的來源項目；取消延期完成勾選時也會同步還原來源項目。

### 3. Google Calendar 唯讀整合

- 使用權限較小的 `calendar.events.readonly` 讀取行程，不會修改或刪除 Google Calendar 原始資料。
- 已知模板依原有欄位建立；其他行程只有在能辨識國文、英文、數學、自然細科或社會科目時才加入 Tracker，完全無法判斷科目的行程會忽略。`本週項目｜名稱` 加入本週項目，Calendar 補做仍加入今日項目。
- 同步包含新增、修改與刪除，避免已刪除的 Calendar 行程留在 Tracker。
- GitHub Actions 可每小時呼叫後端同步，網站不必保持開啟。

### 4. Local-first 與 Supabase 雲端同步

- 所有輸入先保存本機，再排入 Cloud 上傳。
- Cloud 狀態會顯示「同步中」、「待同步 X 天」、「已同步」或「同步失敗」。
- 網路恢復時立即掃描待同步日期並重試，不必等待下一個 10 分鐘週期。
- 雲端資料抵達時若使用者正在輸入，不會強制重畫；失焦後會安全合併，亦可按「立即套用」。
- 同一日期在多分頁／多裝置修改時，以穩定項目 ID 與共同基準交叉比對；不同欄位可合併，同一欄位衝突則保留兩邊並提示處理。

### 5. 帳號復原

- 支援忘記密碼、從 Email 回到 Tracker 設定新密碼，以及重寄註冊驗證信。
- 密碼重設回站網址必須先加入 Supabase Auth 的 Redirect URLs，否則信件能寄出但無法正確回到 Tracker。

### 6. 時間與視覺化

- 今日完成時間、今日數學頁數與本週數學頁數共用切換欄位；切換時欄位會平滑變高或縮短。右側兩個今日完成率分成上下兩列並靠左，整組在欄位中置中且會隨高度平滑移動。數學頁數必須同時具備實際填入並保存的頁碼與完成勾選，Calendar 建議範圍本身不算紀錄。
- 各科完成時間以圓環圖呈現，環中顯示國／英／數／自／社，指向或點擊後顯示分鐘與占比。
- 教材進度圖由標題列直接在目前頁面跳轉；「我的教材」可按國文／英文／數學／自然切換並勾選教材，進度圖只顯示已勾選項目。
- 「學習總結」提供全頁共用的週／月切換；切換時整頁資料區會平滑淡出、調整高度再淡入，週曆／月曆、科目分配、學習趨勢、平均起床時間、上期比較與本期小結會同步使用同一期間。
- 與上期比較整合進本週／本月小結，平均起床時間固定放在小結右側，並顯示相較上週／上月早起或晚起幾小時幾分鐘。
- 週曆／月曆以圓內深淺呈現學習時間、外圍圓環呈現完成率；詳細數字只在指向或點擊日期時顯示。
- 科目分配與學習趨勢各占一半欄位；圓環沿用首頁粗細並將科目字放在環上，點入科目後以同科目深淺色顯示各項目占比、比例與時間，明細以四欄先直向排列。
- 學習總結中的自然科會分成物理、化學、生物與地科；無法辨識細科目的舊紀錄才保留在「自然」。
- 學習趨勢左右分別顯示 `hr` 與完成率刻度；指向時間長條時只顯示整數「X 小時 X 分鐘」。
- 科目分配圓環中央直接顯示本期總分鐘，不另外建立總時數卡片；總結頁會排除重複 Calendar／延期紀錄，不建立第二套資料。
- 本週／本月小結只依學習時數與完成率套用固定評語，不呼叫 AI，也不產生推測性文字。

### 7. 資料安全

- 無法解碼的本機紀錄不會被當成空白覆蓋，可先下載原始備份再用雲端版本修復。
- JSON 匯入採「解析 → 完整驗證 → 預覽 → 確認 → 寫入」，逐日回報本機與 Cloud 結果。
- 清空欄位、刪除項目與新增內容皆保留明確變更語意，避免同步時舊資料復活。
- Cloud／Calendar 設定面板在桌面與手機皆有平順展開／收合過渡，並尊重系統的減少動態效果設定。

### Supabase／Google 一次設定

#### A. Google Cloud

1. 啟用 Google Calendar API。
2. 建立 OAuth 2.0 Web application。
3. OAuth redirect URI 設為：
   `https://xcpnxkkixsxzgbqsiuud.supabase.co/functions/v1/google-calendar-callback`
4. OAuth consent screen 使用 `https://www.googleapis.com/auth/calendar.events.readonly`。
5. OAuth 品牌設定統一使用：
   - 首頁：`https://gsat-study-tracker.liviayeh.dev/`
   - 隱私權政策：`https://gsat-study-tracker.liviayeh.dev/privacy`
   - 服務條款：`https://gsat-study-tracker.liviayeh.dev/terms`

#### B. Vite 前端環境變數

在 GitHub repository 的 **Settings → Secrets and variables → Actions → Variables** 與 Cloudflare Workers Builds 的 **Settings → Builds → Variables and secrets** 都新增：

```text
VITE_GOOGLE_CLIENT_ID=你的 Google OAuth Web Client ID
```

Client ID 是公開識別碼，不是密碼；仍不可把個人專案值直接寫死在原始碼。可參考 `.env.example`。

#### C. Supabase Edge Function secrets

```text
GOOGLE_CLIENT_ID=同一個 Google OAuth Client ID
GOOGLE_CLIENT_SECRET=Google OAuth Client Secret
GOOGLE_REDIRECT_URI=https://xcpnxkkixsxzgbqsiuud.supabase.co/functions/v1/google-calendar-callback
GOOGLE_STATE_SECRET=高熵隨機字串
APP_RETURN_URL=https://gsat-study-tracker.liviayeh.dev/
CALENDAR_CRON_SECRET=32 字元以上隨機字串
```

#### D. Supabase Auth URL Configuration

在 **Authentication → URL Configuration** 設定：

```text
Site URL
https://gsat-study-tracker.liviayeh.dev/

Redirect URLs
https://gsat-study-tracker.liviayeh.dev/
https://gsat-study-tracker.liviayeh.dev/**
```

忘記密碼與 Email 驗證都會回到上述正式網址。

#### E. GitHub Actions 部署

Actions secrets：

```text
SUPABASE_ACCESS_TOKEN
SUPABASE_DB_PASSWORD
CALENDAR_CRON_SECRET
```

Actions variable：

```text
VITE_GOOGLE_CLIENT_ID
```

Public Supabase Project Ref 只由已納入測試的 `supabase/config.toml` 提供，不需要也不應在 GitHub 重複建立 `SUPABASE_PROJECT_ID` Variable／Secret。

推送到 `main` 後，流程會依序執行單元測試、Edge Function 型別檢查、真實瀏覽器 E2E、正式建置、Supabase migration／functions 部署，最後發布 GitHub Pages。

## III. 重大更新版本

### v124｜Google Calendar 排程整合

原先即使已經安排好讀書進度，每天仍需重新輸入紀錄卡。為減少重複整理，開始將 Google Calendar 排程資料整合進系統。

### v133｜Supabase 雲端同步

原先資料保存在單一裝置，換設備後無法延續，因此加入 Supabase 雲端同步，使紀錄可跨裝置保存與讀取。

### v169｜Google Calendar 每小時同步

原先修改 Google Calendar 排程後，紀錄卡不會同步更新，因此串聯 Google Calendar API 與每小時同步機制，排程變動後不必再手動修改紀錄卡。

### v171｜Google OAuth 與正式使用設定完善

測試時發現 Calendar 同步只限測試帳號。完成 OAuth consent、隱私權政策、服務條款、正式網域與發布設定後，其他獲准使用者也能連接與同步。

## IV. 此版本大更新（v171.x）

- 將 Calendar 解析、頁碼優先、合併、延期、完成率、計時與儲存規則逐步移出單一 runtime，建立可測試模組。
- 將本機與 Supabase 存取隔離為 Repository，加入 revision、共同基準、跨分頁鎖與欄位級合併。
- 增加完成率趨勢、教材進度與各科時間圓環圖。
- Calendar 備註統一支援講義版本、冊別、頁碼範圍／單元進度、來源日期、重點與識別碼。

## V. 此版本重要更新（v171.x.xx）

- **v171.6.0-public.9**：「我的教材」改為預設收合、可平順展開的面板；自然教材依物理、化學、地科、生物分組顯示小標，教材名稱不再重複科目；教材進度頁的瀏覽器分頁名稱統一為「每日讀書完成度紀錄卡」。
- **v171.6.0-public.8**：公開版 Google Calendar OAuth 改用權限較小的 `calendar.events.readonly`，同步更新隱私權政策、Supabase 預設值與公開網域設定說明，並補上 OAuth 設定一致性測試。
- **v171.6.0-public.7**：GitHub Pages artifact 改用官方固定名稱 `github-pages`，避免只重新執行失敗的部署 job 時，`github.run_attempt` 已增加但 build artifact 仍沿用前一次名稱，造成部署找不到成品。
- **v171.6.0-public.1**：建立與自用版分離的公開版；移除所有內建日期／星期排程與硬編碼 Calendar fallback，只保留 Google Calendar 實際同步及手動新增項目。教材進度頁新增「我的教材」科目滑塊與勾選管理，進度圖只顯示已選教材。
- **v171.5.14**：補上 Cloudflare Workers 靜態資產部署設定，固定 Node 22 與 Wrangler 版本，直接部署 Vite 產生的 `dist`，不再讓 Wrangler 在 CI 中嘗試自動改寫 Vite 設定而失敗。
- **v171.5.13**：完成勾選加入日期判斷。未延期的過期項目會記錄實際勾選日，未來項目不會提前留下日期；延期項目完成時會記錄延期完成日並同步原日期來源，排程重建後仍保留完成的延期卡。
- **v171.5.12**：修正 Cloud／Calendar 連線設定收回時只有內容淡出、外框最後瞬間縮回的問題。桌面版會同步縮短外框高度，手機版會完整向下收回；動畫結束前保留內容與遮罩狀態，避免畫面突然消失或提早恢復捲動。
- **v171.5.11**：Calendar 匯入改採「科目為最低門檻」。既有教材與固定模板照原規則建立；未知模板只要能辨識國文、英文、數學、物理、化學、生物、地科、自然或社會，仍會建立對應科目的一般卡片，完全無法判斷科目時才忽略。

- **v171.5.10**：Google Calendar 改為僅加入能辨識為既有教材、科目或固定模板的行程。未知標題即使帶有「今日項目／本週項目／補做」前綴也不再建立一般卡片，避免私人行程或格式錯誤的內容進入 Tracker。

- **v171.5.9**：自行新增項目的教材選單補齊六本新講義，並將它們集中顯示於「新增講義」分組。自然科尚未選科目時會先提示可用的新講義，選定物理／化學後只顯示相符教材；切換科目時也會清除不相符的講義，避免錯誤頁碼對照。

- **v171.1.0**：完成率趨勢與教材進度圖正式加入。
- **v171.1.14**：多分頁上傳前交叉比對，保留兩邊有紀錄的內容。
- **v171.1.19**：英文訂正文字改用穩定列 ID，避免輸入中的文字重複保存。
- **v171.1.20**：加入三方合併與刪除墓碑語意，避免刪除項目被同步復活。
- **v171.1.21**：損壞本機資料可備份與修復，匯入驗證加深。
- **v171.1.22**：所有可計時子項目都能獨立計入讀書時間。
- **v171.2.0**：加入刪除確認／小項目 Undo、完整帳號復原、精確 Cloud 狀態、斷線立即重試、輸入安全合併、輸入 debounce、手機 bottom sheet 與 Playwright E2E。
- **v171.2.1**：依紙本目錄確認「新關鍵 1～2 冊」七個單元的頁碼邊界，末頁固定為 p.191，並讓紀錄卡與教材進度圖共用同一份對照資料。
- **v171.2.2**：依紙本目錄確認「新關鍵 3A～4A 冊」七個單元的頁碼邊界，末頁固定為 p.187，並納入共用對照資料。
- **v171.2.3**：修正 Calendar 無法辨識「新關鍵」數學排程；支援 `1-2冊`、`1～2`、`3A-4A冊`、`3A～4A`，並將標準備註的教材、冊別與頁碼安全套用到數學卡片。
- **v171.2.4**：修正同一天兩筆以上數學 Calendar 行程互相覆蓋；原數學排程保留在固定卡片，新增的「新關鍵」等不同教材／範圍會成為可見且可分別紀錄的項目，星期日也不再遺漏數學行程。
- **v171.2.5**：移除合併大卡內各子卡片重複顯示的標題；教材、冊別、頁碼、單元、勾選、時間與延期控制皆保留。
- **v171.2.6**：子卡片不顯示 `p.x–y` 頁碼標題；Calendar 指定的數學講義版本改為鎖定顯示，避免被手動改錯。
- **v171.2.7**：「教材進度圖」改為在目前分頁直接跳轉，不再另外開啟新分頁。
- **v171.2.8**：今日／本週數學頁數與教材進度改用實際頁碼紀錄；勾選 Calendar 建議頁數不再直接增加統計。
- **v171.2.9**：頁數統計增加完成條件；只有「實際頁碼紀錄＋已勾選完成」同時成立才會計入。
- **v171.2.10**：「英文訂正與搭配詞整理」改為離開文字欄位時才儲存，輸入期間不再反覆寫入紀錄。
- **v171.2.11**：「其他補充」改為離開欄位後才儲存，長篇輸入期間不再反覆寫入紀錄。
- **v171.2.12**：所有文字、數值與時間輸入欄位統一改為離開欄位後才儲存；輸入期間仍即時更新畫面摘要。
- **v171.2.13**：勾選 Calendar 數學項目後立即以實際紀錄頁碼更新今日／本週頁數；Calendar 建議頁碼不作為完成頁數，並相容舊版實際頁碼標記。
- **v171.3.0**：新增「學習總結」頁面，以唯一的週／月全域滑塊同步控制週曆／月曆、科目分配、學習趨勢、平均起床時間、與上期比較及本期小結。
- **v171.3.1**：移除學習總結主標題左側的裝飾圖案，讓標題與副標題直接靠左呈現。
- **v171.4.0**：完成學習總結日曆視覺、日期 tooltip、科目圓環下鑽與固定規則小結；首頁計時可從手動分鐘數換算至最接近秒數後接續。
- **v171.4.1**：科目圓環下鑽加入向左過渡動畫與較細的小圓環；明細改為大字雙列，移除重複科目名稱，並以講義版本與冊別區分講義項目。日期圓心與完成率外圈改用獨立藍／綠色系，100% 完成不改變圓心時數深淺。
- **v171.4.2**：徹底分離日期圓心與完成率外圈的顏色圖層，圓心改用依時數算出的不透明藍色，避免外圈顏色透入；「回到 Tracker」改為位於週／月滑塊上方的醒目按鍵；科目分配時間統一改用小時並顯示到小數點後一位。
- **v171.4.3**：同步更新學習總結的端對端測試，改以 `1.0 hr` 驗證一小時總學習時間，不再尋找改版前的 `60` 分鐘文字。
- **v171.4.4**：移除科目分配的「返回全部科目」按鍵；進入科目明細後，直接點擊小圓環即可返回全部科目，並支援鍵盤 Enter／空白鍵操作。日期完成率達 100% 時，外圈固定使用 `#70DE43`。古今悅讀一百等分回教材在科目分配中依教材名稱合併統計。
- **v171.5.0**：重整學習總結版面，擴大科目分配欄位，科目明細每列四項；將與上期比較併入本週／本月小結，平均起床時間改置於小結右側並顯示早晚差；持平評語使用淺藍底；CNN 互動英文與常春藤在英文明細中合併為「雜誌」；小圓環返回全部科目時加入放大回位動畫。
- **v171.5.1**：「回到 Tracker」與主頁的排程建議、學習總結及教材進度按鍵共用同一份樣式，確保桌面、手機、滑鼠指向與鍵盤焦點外觀完全一致。
- **v171.5.2**：修正科目圓環點擊範圍互相遮擋，並依大小圓環的實際位置製作返回動畫；科目明細固定四項一列，科目分配與學習趨勢各占一半，趨勢加入 `hr` 刻度與整數時分提示；自然拆分為物理、化學、生物與地科；移除主頁週五／週日本週趨勢；持平小結統一依 ±5% 規則套用淺藍底。
- **v171.5.3**：首頁三項學習成果切換加入高度與內容過渡，右側兩個今日完成率改為橫向置中並隨高度平滑移動；學習總結週／月切換加入整頁淡出、變高／縮短與淡入動畫；科目明細改為四欄先直向排列。
- **v171.5.4**：修正科目明細使用瀏覽器自動分欄後仍橫向排列的問題；改由項目數計算列數，再用固定網格強制先向下、後向右排列，並通過完整瀏覽器測試。
- **v171.5.5**：依介面需求更正首頁完成率排版；「原訂今日項目完成率」與「今日總項目完成率」恢復上下兩列並靠左，整組仍在右側欄位中央，且保留切換統計時的平滑位移動畫。同時修正學習總結切換週／月後被舊淡出動畫鎖在透明狀態的問題。
- **v171.5.6**：學習總結的日期提示加入淡入位移動畫；科目明細改為每欄最多四項、先向下再向右排列。桌機與手機進入單科目時，圓環皆依實際位置由中央縮小並移至左側，明細固定顯示於右側；手機頁首的標題字級與返回按鍵位置同步教材進度圖頁面，週／月滑塊改用頁面一致的學習時數藍色。
- **v171.5.7**：依教材目錄新增化學「領航」、物理「優勢／逆轉勝」、英文「學測週計畫／混合題30篇實戰演練」與數學A「新大滿貫」的辨識及頁碼地圖；Calendar 可用固定識別碼選到正確講義，手動卡片、章節提示與教材進度圖共用同一份資料。逆轉勝來源僅提供週次與單元，因此先建立 p.1–255 全書範圍，不推測各單元頁界。
- **v171.5.8**：Cloud／Calendar 連線設定視窗加入淡入、位移與縮放的展開／收合動畫，手機底部面板同步套用；教材進度圖的「回到 Tracker」改為與學習總結共用完全相同的按鍵樣式。六本新教材的教材主碼亦可加日期或課次尾碼，在保持每筆 Calendar 識別碼唯一時仍能辨識正確教材。

## VI. 開發

需求：Node.js 22。

```bash
npm ci
npm run typecheck
npm test
npx playwright install chromium
npm run test:e2e
npm run build
```

Cloudflare Workers Builds：

```text
Build command: npm run build
Deploy command: npx wrangler deploy
Root directory: /
```

`wrangler.jsonc` 會直接部署 `dist` 靜態資產；本機也可使用 `npm run deploy:cloudflare` 完成建置與部署。

本機環境設定：

```bash
copy .env.example .env.local
```

Edge Functions 型別檢查另需 Deno：

```bash
npm run typecheck:edge
```

## VII. 專案結構

```text
src/
├─ application/       Calendar → StudyTask 等流程
├─ calendar/          Calendar parsing 與排程摘要
├─ data/              教材、章節與頁碼資料
├─ storage/           Local／Supabase Repository、同步與 migration
├─ study/             完成率、延期、合併、計時等純邏輯
├─ ui/                可重用 UI renderer
├─ legacy-app.ts      尚在漸進拆分的相容性 runtime
├─ main.ts
└─ styles.css
supabase/
├─ functions/         Google Calendar OAuth／同步
└─ migrations/        資料庫版本
tests/                單元與回歸測試
e2e/                  Playwright 真實瀏覽器流程
.github/workflows/    驗證、Supabase 與 GitHub Pages 部署
```

## VIII. 注意事項

- 不要把 `GOOGLE_CLIENT_SECRET`、`GOOGLE_STATE_SECRET`、`CALENDAR_CRON_SECRET`、Supabase service role key 或資料庫密碼放進前端或 Git。
- Supabase publishable key 與 Google OAuth Client ID 是前端公開識別碼，但應由既定設定流程管理。
- 瀏覽器若禁止 persistent storage，Tracker 會顯示警告；此時不要把「已暫存」理解成關頁後仍一定存在。
- Google Calendar 只讀 scope 不代表同步後的 Tracker 副本不含個人資料；資料刪除與隱私權政策仍需和實際行為一致。
- 建置若只出現 JavaScript chunk size 警告，不會阻止發布；後續可繼續拆分 `legacy-app.ts` 以降低載入與修改風險。
